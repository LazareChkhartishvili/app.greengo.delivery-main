# Laravel Backend Environment Configuration
# Copy this content to your Laravel backend .env file

APP_KEY=base64:oXmglA5WkYFRktmbQqjgQozGExWnt949a6Y8wKXPJvA=
APP_NAME=Laravel
APP_ENV=production
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=https://greengo-api-production.up.railway.app

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration - Update these for your production database
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=greengo
DB_USERNAME=postgres
DB_PASSWORD=lazare2002

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting, Filesystem, Queue
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Cache Configuration
CACHE_STORE=database
CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

# Redis Configuration
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS Configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"

# CORS Configuration for Frontend
# Add these to your Laravel backend .env for CORS configuration
FRONTEND_URL=https://greengodelivery-ge-main.vercel.app
SANCTUM_STATEFUL_DOMAINS=greengodelivery-ge-main.vercel.app,localhost:3001,localhost:4001

# For development, you may also need:
# CORS_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:4001,https://greengodelivery-ge-main.vercel.app
# SESSION_DOMAIN=.localhost
